{% extends 'base.html.twig' %}

{% block title %} Message envoyé{% endblock %}

{% block body %}
<h1>Merci pour votre demande</h1>
<p>Votre message a bien été envoyé, Je vous recontacterai sous peu.</p>

<h2>Résumé :</h2>
<ul>
{% if data.type == 'contact' %}
    <li>Date d'envoi de la demande : {{ data.submitDate|date('d/m/Y') }}</li><br>
    <li>Nom : {{ data.fullName }}</li><br>
    <li>E-mail : {{ data.email }}</li><br>
    <li>Téléphone : 
        {% if data.telephone is not empty %}
            {{ data.telephone }}
        {% else %}
            Non renseigné
        {% endif %}</li><br>
    <li>Objet de votre demande : {{ data.subject }}</li><br>
    <li>Message : {{ data.message }}</li><br>
{% endif %}
</ul>
<a href="{{path('home_index')}}">Revenir au site</a>
{% endblock %}
